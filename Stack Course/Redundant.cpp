#include<iostream>
#include<stack>
using namespace std;

bool Redundant(string &p)
{
    stack<char>s;
 for (int i = 0; i < p.length(); i++)
 {
     char ch=p[i];
    if(ch =='(' || ch=='+' || ch=='-' || ch=='*'){
        s.push(ch);
    }
else{
   if() 
}
 }
}
int main(){
    
    return 0;
}